# Devin RV Zero Test

This repository is a zero-flow verification target.

The `main` branch intentionally contains only the automation bootstrap. The first application code should enter through a pull request so the reusable Devin automation workflow can review it before merge.
